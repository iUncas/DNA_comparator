package college;
interface frequencyinterface {
	void one();
  static double getEnzimesFrequency(int frequency, int chainlenght) {
	  double fist = (double)frequency / (double)chainlenght;
	  return fist;
	  
  }
}